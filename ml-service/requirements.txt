# --- Core API ---
fastapi==0.109.2
uvicorn[standard]==0.27.0
httpx==0.26.0
pydantic==2.6.0
python-dotenv==1.0.1
python-multipart==0.0.9  # สำคัญมาก! ใช้สำหรับรับไฟล์รูปจากแอปมือถือ

# --- Image Processing ---
numpy==1.26.4
opencv-python-headless==4.9.0.80  # ใช้ headless ถูกแล้ว เพื่อให้รันบน Server ได้โดยไม่พัง
pillow==10.2.0

# --- Machine Learning ---
# ใช้ tensorflow-cpu ดีที่สุดแล้วสำหรับการนำไป Deploy บน Render (ประหยัดพื้นที่และเซิร์ฟเวอร์ไม่มี GPU)
tensorflow-cpu==2.18.0
scikit-learn==1.4.0